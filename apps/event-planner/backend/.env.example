# Auth (PocketBase — shared across apps)
POCKETBASE_URL=http://pocketbase:8090

# Internal credit API (proxies to Launchpad backend, the credit authority)
LAUNCHPAD_URL=http://launchpad-backend:8000

# App-specific
DB_PATH=/app/data/event_plans.db
